---
id: TASK-20260622-029
status: Ready
human_owner: 김민혁
human_approver: 이상혁
primary_agent: 김민혁의 AI(Claude Code/Cursor)
review_agent: Claude(통합) · Codex
related_screens: [스튜디오]
related_requirements: []
allowed_paths: [apps/miniapp/src/audio/**]
forbidden_paths: [apps/api/**, apps/miniapp/src/routes/**, apps/miniapp/src/lib/**, apps/miniapp/src/theme.css, 산출물/**]
---

# TASK-20260622-029 — 버그: 코드/음 음정이 안 맞음 (오디오 엔진)

> 김민혁: 클론(또는 pull)한 repo에서 네 AI에게 이 파일을 통째로 줘.

## 0. 먼저 읽기
`CLAUDE.md` · `산출물/00_프로젝트관리/인수인계_현재상태.md` · `산출물/00_프로젝트관리/회의록/2026-06-22_팀회의.md` · `apps/miniapp/src/audio/engine.ts` · `chordReducer.ts` · `transport.ts` · `chordReducer.test.ts`.

## 1. 배경 / 증상
6/22 회의에서 **"코드마다 음이 안 맞음"** 확인 — 재생 시 코드(C·G·F·Am 등)·멜로디 음의 **피치가 틀리게** 들림("음 확인 → 음이 잘못되었음").

## 2. 목표
- 코드/멜로디 재생 시 **정확한 음정**이 나도록 수정.
- 원인 후보: ① 코드→구성음 매핑(`chordReducer.ts`) ② 음이름→주파수 변환(`engine.ts`, 평균율/옥타브/A4=440Hz) ③ 재생 타이밍(`transport.ts`).

## 3. 비목표
- 클라 UI/라우트 수정 금지(이상혁/커뮤니티 스트림). 백엔드 수정 금지(그건 네 TASK-20260623-001). 새 악기/음색 추가는 별건.

## 4. 수락 기준
- 각 코드·음이 기준 피치(평균율, A4=440Hz)와 일치(청취 + 가능하면 주파수 단언).
- **단위테스트로 음정 검증 추가/보강**(`chordReducer.test.ts`) → `npm --prefix apps/miniapp run test` **기존 7개 유지 + 신규 통과**.
- 실제 폰/웹(`dev:web`)에서 코드별·멜로디 청취 확인.

## 5. 변경 허용/금지
- 허용: **`apps/miniapp/src/audio/**` 만**.
- 금지: 그 외 전부(특히 `routes/**`·`lib/**`·`theme.css`·`apps/api/**`).
- **오디오 엔진의 공개 API(함수 시그니처)는 유지** — `Studio.tsx`가 그대로 호출하도록. 시그니처를 바꿔야 하면 조장에게 결정요청(겹침 방지).

## 6. 검증
- `npm --prefix apps/miniapp run test` (음정 단위테스트) · `npm --prefix apps/miniapp run dev:web` 청취.

## 7. 충돌 방지 (경로 락)
- 분기 기준: **`feat/TASK-20260621-027-gesture-spike`** 에서 분기(주의: `develop`엔 아직 앱 코드 없음). 새 브랜치 `fix/TASK-20260622-029-audio-pitch`.
- 작게 자주 commit. develop/메인 머지는 **조장만**. 작업 전 `git pull`.
- **경로 분담:** 너=`apps/miniapp/src/audio/**`(이 버그) + `apps/api/**`(TASK-001 백엔드). 이상혁/Claude=커뮤니티 프론트(`routes/**`·`lib/share.ts`·`theme.css`). → **겹치지 않음.**

## 8. 사람 승인
- merge·push는 git 규칙(조장 게이트) 따름. **pem/ppk·비밀값 저장소 금지.**
