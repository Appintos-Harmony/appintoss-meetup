---
id: TASK-20260623-010
status: AI Review
human_owner: 이상혁
human_approver: 이상혁
primary_agent: Claude Code
review_agent: Codex(독립검증 · 사람 구동 대기)
related_requirements: []
related_screens: []
related_adrs: []
allowed_paths: [tooling/scripts/**, .claude/commands/**, 평가증빙/루프실행_로그/**, 산출물/09_AI개발파이프라인/**, .gitignore]
forbidden_paths: [apps/**, .claude/settings.json, tooling/git-hooks/**, CLAUDE.md, AGENTS.md]
---

# TASK-20260623-010 · 루프 엔지니어링(자율 엔지니어링 루프) 구축

## 배경과 문제
하네스의 척추(상태기계·`검증_전체`·`커밋.mjs`·git 훅·서브에이전트)는 갖춰졌으나, 이를 묶어 자동으로 도는 **오케스트레이터**가 없었다. "AI를 라이프사이클 전반에 쓰고 재사용 가능한 파이프라인으로 운영"(과제 AI 25점)을 증거로 보이려면, 안전 게이트가 코드로 강제되는 반복 가능한 엔지니어링 루프가 필요하다.

## 목표
- 결정론 엔진(외부 의존성·API 없음, 순수 Node)으로 다음을 강제한다: Ready 작업 선택 · 경로안전 · 코드 게이트(tsc+vitest) · 변경 범위 가드 · 반복 캡.
- Claude Code 내장 `/loop`+서브에이전트로 AI 스테이지(구현·검토)를 구동한다(별도 유료 API 키 코드 없음).
- 모든 비협상 안전 게이트(푸시/배포=사람, green-only 커밋, 보호구역 불가, 정직성)를 프롬프트가 아니라 **코드**로 건다.

## 비목표
- 무인 야간 cron(P4). 본 작업은 감독 주간 v1까지.
- apps/api용 자동 게이트. 현재 api 코드경로는 HARD STOP으로 사람에게 넘긴다.
- 진짜 교차검토(Codex) 자동 연동. 본 루프의 REVIEW는 same-family 초안이며 사람이 Codex를 별도 구동한다.

## 입력 근거
- 워크플로 타당성 분석(12에이전트, Conditional YES) · 본 세션.
- CLAUDE.md §7(상태기계·결과계약), `.claude/rules/git.md`(DL-012 커밋=자동/푸시=사람), `documents.md`(HUMAN_APPROVAL 보호).

## 수락 기준
- [x] `작업_인덱서.mjs` · TASK 파싱(누락 0)·Ready 큐·경로안전(공유 canonicalize)·결정론 선택.
- [x] 코드 게이트 · `검증_전체 --code`(`코드검증.mjs`, 사람 병렬 커밋 7f3c5fc) 사용. 중복인 `코드게이트.mjs`는 **폐기**. 실측 tsc·vitest 58/58 PASS.
- [x] `루프_가드.mjs` · preflight·check-diff(--staged)·단조 반복캡. 보호경로·forbidden·HUMAN_APPROVAL·무게이트코드 강제.
- [x] `커밋.mjs --guard-task` · 커밋 경계에서 diffGuard 재강제(위반 시 자동 unstage).
- [x] 단위테스트 22건 PASS(`node tooling/scripts/루프엔진_검증.mjs`, `node:test` 무의존성. REVIEW-035·036 회귀 포함).
- [x] `/loop-engineer` 커맨드 · 8스테이지·하드캡·정직성·사람게이트.
- [x] 로그 sink(`평가증빙/루프실행_로그/`)·설계 문서.
- [x] 독립검토(적대적 워크플로) · [REVIEW-035](../독립검토서/REVIEW-20260623-035_루프엔지니어링_검토.md): blocker 다수 발견·실증·조치 후 재검증 PASS.
- [x] B2 완전 백스톱(pre-commit 훅) · 라이브 검증, 커밋 `3471e23`.
- [x] Codex 진짜 교차검토 · [REVIEW-036](../독립검토서/REVIEW-20260623-036_Codex_루프엔지니어링검토.md)(No-Go→수정) → [REVIEW-037](../독립검토서/REVIEW-20260623-037_Codex_루프엔지니어링검토_라운드2.md) **Conditional Go**. §7.4 2계열 독립성 닫힘. 조건: 무인 cron 시 서버측 branch protection.

## 변경 허용 범위
`tooling/scripts/**`, `.claude/commands/**`, `평가증빙/루프실행_로그/**`, `산출물/09_AI개발파이프라인/**`, `.gitignore`.

## 금지 범위
`apps/**`(제품 코드), `.claude/settings.json`(권한 자가확대 금지), `tooling/git-hooks/**`(게이트 자가수정 금지), 헌장 문서.

## 예상 영향과 위험
- 권한: 무인 실행 시 `git checkout` 미허용으로 멈출 수 있음 → 사람이 `settings.json`에 `Bash(git checkout:*)` 추가 필요(AI 자가확대 불가). 코드게이트는 `node tooling/scripts/*` 허용목록으로 이미 커버.
- 선택 로직은 lowest-id 단조 · 큰 코드 TASK가 먼저 잡힐 수 있어 감독 주간 권장.

## 검증 명령
```bash
node tooling/scripts/루프엔진_검증.mjs        # 단위테스트 16건
node tooling/scripts/검증_전체.mjs --code      # 문서·링크·비밀값 + tsc + vitest(58)
node tooling/scripts/작업_인덱서.mjs           # Ready 큐
```

## 필요한 테스트 증거
`평가증빙/루프실행_로그/`의 실행 로그(게이트 원문 출력), 단위테스트 PASS 출력.

## 롤백 또는 복구 방법
신규 파일 추가(제품 코드 무변경) · 브랜치 폐기 또는 해당 커밋 revert로 완전 복구.

## 사람 승인 지점
- `settings.json` 권한 1줄 추가(무인 승급 시).
- develop 머지·푸시.
- Codex 진짜 교차검토 구동.
