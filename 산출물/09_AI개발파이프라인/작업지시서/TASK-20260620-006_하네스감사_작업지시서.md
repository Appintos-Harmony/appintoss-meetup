# TASK-20260620-006 하네스 감사 작업지시서

```yaml
id: TASK-20260620-006
status: Closed
human_owner: 이상혁
human_approver: 이상혁
primary_agent: Codex
review_agent: Claude Code
related_requirements: []
related_screens: []
related_adrs: []
allowed_paths: ["산출물/09_AI개발파이프라인/독립검토서/REVIEW-20260620-006_하네스감사.md"]
forbidden_paths: ["apps/**", "packages/**", "infra/**", "CLAUDE.md", "AGENTS.md", "CHATGPT_PRO_운영지침.md", ".claude/**", ".agents/**", "프로젝트_운영/**", "산출물/00_**", "산출물/02_**"]
```

## 배경과 문제

작업패킷 본격 착수 전, 멀티에이전트 하네스가 `CLAUDE.md` §7·§8·§9 명세대로 구성·작동하는지 **독립 검증**이 필요하다. Claude Code의 자가진단(아래)을 Codex가 확인 또는 반증한다.

## 목표

- 하네스 구조·스킬·계약·스크립트·자동화의 실제 상태를 증거 기반으로 판정한다.
- Claude 자가진단의 각 항목을 confirm/dispute 한다.

## 비목표

- 누락 항목을 이번 작업에서 구현하지 않는다(감사만). 수정은 합의 후 별도 TASK.

## 입력 근거

- `CLAUDE.md` §7(하네스)·§8(저장소 구조)·§9(필수 산출물)·§7.7(자동화)
- `AGENTS.md`, `산출물/09_AI개발파이프라인/*`, `프로젝트_운영/*`, `tooling/scripts/*`

## Claude 자가진단 (Codex가 confirm/dispute)

- C1[P1] `.claude/` 전체 부재(agents 9·skills 5·rules 4·settings.json)
- C2[P1] §7.7 자동화 대부분 미구현(CI·secret scan·링크검사·구현자=검토자·증거링크)
- C3[P2] Codex 스킬 `prepare-test-evidence` 누락(§7은 3개, 실제 2개)
- C4[P2] git 미초기화 → diff/PR/worktree 흐름 비활성
- C5[P3] `09`의 하네스_구성도·라이프사이클_평가서·재사용_운영지침 미작성
- C6[info] CLAUDE.md §7 ↔ 실제 불일치(prepare-test-evidence)

## 수락 기준

- [ ] §8 폴더 구조와 실제 트리를 대조한 표(존재/누락)
- [ ] `.claude/`·`.agents/skills/` 항목별 존재·내용 평가
- [ ] `산출물/09` 계약·템플릿 존재·정합 평가
- [ ] 두 스크립트 **실제 실행** 결과(명령·종료코드·핵심 출력)
- [ ] §7.7 자동화 항목별 구현/미구현 판정
- [ ] §9 필수 산출물 매핑 대비 실제 파일 존재/누락 집계
- [ ] §8.1 경로 규칙 위반(영문 docs/·중복본) 점검
- [ ] C1~C6 각 항목 confirm/dispute + 근거
- [ ] 종합 판정: 하네스 `사용 가능 / 조건부 / 불가` + 우선 수정 목록

## 검증 명령

```bash
node tooling/scripts/세션_시작_컨텍스트.mjs
node tooling/scripts/팀공유문서_검증.mjs --allow-template
git status   # 저장소 여부 확인
```

## 필요한 테스트 증거

명령 원문 출력, 파일 경로(가능하면 라인), 트리 목록. 실행 못 한 항목은 `Not Run`, 접근 불가는 `Not Available`, 미확인은 `Unverified`.

## 금지 범위

저장소 수정·생성(자기 독립검토서 1개 제외)·commit·push·merge·migration·배포. 추정을 사실로 기재 금지.

## 롤백 또는 복구 방법

해당 없음(읽기 전용). 독립검토서만 추가.

## 사람 승인 지점

감사 결과·우선 수정 목록은 이상혁이 확인 후 수정 TASK 승인.
