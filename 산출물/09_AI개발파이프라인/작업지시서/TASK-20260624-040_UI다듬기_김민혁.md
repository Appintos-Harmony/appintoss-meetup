# TASK-20260624-040 작업지시서 · 미니앱 UI 다듬기 + 설정 법적고지 모달

```yaml
id: TASK-20260624-040
status: AI Review
human_owner: 김민혁
human_approver: 이상혁(조장·develop 머지) · 양록빈(QA 시각 회귀)
primary_agent: 김민혁(Cursor·사람 주도)   # 커밋 계정 lonis = 김민혁
review_agent: Claude Code (REVIEW-20260624-041)
related_requirements: []   # 커뮤니티/홈/설정(법적고지=POL)/스튜디오. 요구사항_추적표 갱신 시 ID 부여(현재 드리프트)
related_screens: [홈, 커뮤니티, 설정, 온보딩, 스플래시, 스튜디오]   # SCR ID 단일체계 통일 후 매핑
related_adrs: []   # 리스크①(이용약관·개인정보 준비중) 대응
allowed_paths:
  - apps/miniapp/index.html
  - apps/miniapp/public/
  - apps/miniapp/src/components/studio/
  - apps/miniapp/src/lib/legal.ts
  - apps/miniapp/src/routes/
  - apps/miniapp/src/theme.css
  - 산출물/03_화면설계/
  - 프로젝트_운영/00_팀공유/00_진행사항.md
forbidden_paths:
  - apps/api/
  - packages/
  - infra/
  - 산출물/04_시스템설계/
  - 산출물/05_개발설계/
  - .claude/
  - tooling/
  - CLAUDE.md
```

> 주의: **소급 작성.** 구현·푸시(브랜치 `feat/TASK-20260624-040-ui-polish`, 11커밋, tip `85146e5`, 작성자 lonis=김민혁) 이후, 하네스 절차를 보강해 DL-014 develop 머지 조건(조장 승인·`검증_전체 --strict`·독립검토서)을 채우기 위한 문서다.

## 배경과 문제

미니앱이 동작은 하나 버튼 줄바꿈/넘침, 칩바 라벨 잘림, 커뮤니티/홈 페이징 부재 등 거친 부분이 남아 있었다. 또한 **설정에 이용약관·개인정보 처리방침이 노출되지 않아 리스크①(약관·개인정보 '준비중' = 출시 블로커)**가 열려 있었다.

## 목표

- 미니앱 전반 UI 다듬기: 아이콘+글자 버튼 줄바꿈/넘침 전역 차단, 커뮤니티·홈 정렬·페이징, 로고/아이콘 정정.
- 설정에 이용약관·개인정보 처리방침 **초안**을 in-app 모달로 노출(리스크① 부분 완화).

## 비목표

- 도메인 로직·API·테이블 변경(무변경 유지).
- 신규 핵심기능 추가(§17: 6/22 이후 금지).
- 약관·개인정보의 **법적 확정**(법무 검토·운영주체 확정은 별도 트랙).

## 입력 근거

- [리스크_관리대장](../../00_프로젝트관리/리스크_관리대장.md) ①
- [2026-06-23 일일회의](../../00_프로젝트관리/회의록/2026-06-23_일일회의_발표전_진행공유.md)
- 화면명세서(스튜디오·커뮤니티)

## 수락 기준

- [x] `tsc --noEmit` 0건 (실측 PASS, worktree)
- [x] `vitest run` 58/58 (실측 PASS)
- [x] `vite build` PASS (646.30kB)
- [x] 도메인/API/테이블 무변경 (diff = CSS·뷰 레이어 한정)
- [x] `legal.ts` '초안 · 정식 시행 전 법무 검토 필요' 정직 표기
- [ ] 독립검토서 [REVIEW-20260624-041](../독립검토서/REVIEW-20260624-041_UI다듬기_TASK040_검토.md) 사람 사인오프
- [ ] 조장(이상혁) develop 머지 승인 + `검증_전체 --strict` PASS (DL-014)

## 변경 허용 범위 / 금지 범위

위 `allowed_paths` / `forbidden_paths` 참조. 실제 변경 18파일은 전부 allowed 안.

## 예상 영향과 위험

- 번들 JS 598.9 → **646.3kB(+약47)**, `logo.png` 357KB 추가. 코드분할 없음 → 첫 로딩 지연.
- 드럼 landscape 4열 그리드·`z-index isolation`은 단순 다듬기를 넘는 **레이아웃 구조 변경 경계선(§17 동결)** → 시각 회귀 검수 필요.
- **P2:** 커뮤니티 페이저/정렬 토글이 `stopPreview()` 미호출(REVIEW-041 F1).
- `manifest.webmanifest`/PWA 메타는 토스 WebView(App-in-App)에서 실효 불명.

## 검증 명령

```bash
node tooling/scripts/코드검증.mjs            # tsc --noEmit + vitest run
cd apps/miniapp && npx vite build            # 번들 크기
node tooling/scripts/검증_전체.mjs --strict  # 머지 전(DL-014)
```

## 필요한 테스트 증거

- 코드검증 PASS 로그(tsc 0·vitest 58/58), `vite build` 번들 크기(646.30kB) · 실측 확보.
- 실기기 시각 회귀(회전·바텀시트 z-index 가림) · **Not Run**(추가 필요).

## 롤백 또는 복구 방법

- 현재 develop **미머지** → 브랜치 보류 시 영향 0.
- 머지 후 문제 시 머지 커밋 `git revert` 또는 develop 직전 tip(`6bfd97f`)으로 리셋.

## 사람 승인 지점

- **이상혁**: develop 머지 승인(DL-014 3조건).
- **양록빈**: 시각 회귀 RT 추가 후 육안 검수.
- **F1(stopPreview)**: 머지 전 1줄 수정 포함 vs 후속(P2) · 사람 결정.
