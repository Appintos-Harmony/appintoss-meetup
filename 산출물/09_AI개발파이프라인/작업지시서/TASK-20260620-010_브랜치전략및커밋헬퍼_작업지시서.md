# TASK-20260620-010 브랜치 전략 및 커밋 헬퍼 정합

```yaml
id: TASK-20260620-010
status: Done
human_owner: 이상혁
human_approver: 이상혁
primary_agent: Claude Code
review_agent: Codex
related_requirements: []
related_screens: []
related_adrs: []
allowed_paths: [".claude/rules/git.md", "산출물/09_AI개발파이프라인/재사용_운영지침.md", "tooling/scripts/커밋.mjs", "README.md"]
forbidden_paths: ["apps/**","packages/**","프로젝트_운영/**","평가증빙/**","산출물/00_**","산출물/02_**"]
```

> TASK-009를 대체(superseded)한다. 이상혁 결정 1~7 반영. **구현은 사람 승인 후**(바로 수정 금지).

## 채택된 사람 결정 (이상혁, 2026-06-20)
1. 부트스트랩 첫 커밋 1회는 `main` 허용. 2. 이후 `main`은 조장 승인 없이 merge 금지. 3. `develop`=개발 통합/백업. 4. 작업 브랜치=TASK 단위. 5. `git add -A` 허용하되 **커밋 전 `git status` 원문 확인 필수**. 6. settings.json 권장 권한 적용 승인. 7. 첫 커밋 승인.

## 제안 브랜치 전략

| 브랜치 | 용도 | 분기 출발 | 병합 대상 | 직접 커밋 |
|---|---|---|---|---|
| `main` | 출시·안정(항상 배포 가능) | - | - | 부트스트랩 1회만. 이후 금지 |
| `develop` | 개발 통합/백업 | main | main(릴리즈 시) | 지양(작업 브랜치 경유) |
| `feature/TASK-…-슬러그` | 기능(TASK 단위) | develop | develop | 허용 |
| `fix/TASK-…-슬러그` | 버그 수정 | develop | develop | 허용 |
| `chore/…` | 빌드·도구·설정 | develop | develop | 허용 |
| `docs/…` | 문서 전용 | develop | develop | 허용 |
| `release/날짜` | 출시 후보 안정화 | develop | main + develop | 허용(안정화만) |

흐름: `feature/fix → (독립검토) → develop → (안정화) release/… → main`

## main 병합 조건 (명시)
`main`으로의 병합은 다음 3가지가 모두 있어야 한다:
1. **이상혁(조장) 승인**
2. **검증 결과**: `node tooling/scripts/검증_전체.mjs --strict` PASS(해당 단계 기준)
3. **독립검토서**: 해당 변경의 `REVIEW-…`(다른 계열 AI)

## 커밋 헬퍼(`커밋.mjs`) 변경
- **main 보호:** 커밋이 1개 이상 존재하는데 현재 브랜치가 `main`이면 **차단**. `--allow-main`은 **커밋 0개(진짜 부트스트랩)** 일 때만 유효.
- **스테이징(결정 5):** `git add -A` 유지. 단 커밋 직전 `git status --short` **원문을 출력**하고, 비-conventional·예상 외 파일이 있으면 경고. (`--paths` 옵션은 선택적으로 함께 제공)
- conventional 메시지 검증·pre-commit 비밀값 훅·푸시 미수행은 유지.
- pre-push: `main` 직접 push 차단 유지(`develop`·작업 브랜치는 허용).
- **Edge case(REVIEW-009 반영):** 스테이징은 `execFileSync('git',['add','--',...paths])` 배열 인자로(셸 escaping·공백·한글 경로 안전). `--path <p>` 반복 허용. detached HEAD(`symbolic-ref` 실패) 중단. 빈 스테이징(`git diff --cached --quiet`) 중단. 기존 staged 변경 있으면 기본 중단(`--include-staged` 시만 허용). `..`·절대경로·`.git` 내부 경로 차단. 커밋 직전 `git diff --cached --name-status` + `git status --short` 출력. message body는 `--body` 별도 옵션.

## 수락 기준
- [ ] `.claude/rules/git.md`·재사용_운영지침에 위 브랜치 전략·main 병합 조건 반영
- [ ] `커밋.mjs`: 커밋 존재 시 main 차단, 부트스트랩(커밋 0)만 `--allow-main` 허용
- [ ] `커밋.mjs`: 커밋 전 `git status --short` 원문 출력
- [ ] 회귀: `검증_전체` PASS
- [ ] 변경 자체는 작업 브랜치에서 수행(메타: 이 정책 도입 후 첫 적용 사례)

## 변경 예정 파일 목록 (구현 시 · 현재는 제안만)

| 파일 | 변경 | 비고 |
|---|---|---|
| `tooling/scripts/커밋.mjs` | 헬퍼 강화(아래 8조건) | 핵심 구현 |
| `.claude/rules/git.md` | 브랜치 전략 + main 병합 조건 정식 문서화(기존 커밋/푸시 정책과 통합) | **브랜치 전략 정본** |
| `산출물/09_AI개발파이프라인/재사용_운영지침.md` | 부트스트랩 순서에 develop 생성·브랜치 전략 추가 | 재사용 |
| `README.md` | 팀원용 브랜치 한 줄 안내(작업=feature/fix/…→develop, main 직접 금지) | 온보딩 |

### 커밋 헬퍼 8조건 (Codex REVIEW-009 + 이상혁 승인)
① 커밋 존재 시 `main` 차단(부트스트랩 0커밋만 `--allow-main`) · ② `--path <p>` 반복 + `execFileSync('git',['add','--',...paths])` · ③ 빈 스테이징 차단(`git diff --cached --quiet`) · ④ detached HEAD 차단(`symbolic-ref` 실패) · ⑤ 기존 staged 변경 기본 차단(`--include-staged` 시만 허용) · ⑥ repo 밖·절대경로·`..`·`.git` 경로 차단 · ⑦ 커밋 직전 `git diff --cached --name-status` + `git status --short` 출력 · ⑧ `git add -A`는 `--all` 명시 옵트인(결정 5: 허용하되 status 확인 필수)

## 사용자 승인 (2026-06-20, 이상혁)
브랜치 전략(가벼운 GitFlow + TASK), `--path` 반복, 빈 스테이징·detached HEAD·기존 staged 기본 차단, 부트스트랩 후 develop 생성 = **승인됨**. 브랜치 전략 정본은 `.claude/rules/git.md`. **구현 착수 최종 go만 대기.**

## 금지 / 승인
allowed_paths 밖 변경 금지. 실제 merge·push·배포 금지. 구현은 이상혁 최종 go 후 → Codex 독립검토(REVIEW-)로 닫는다.
