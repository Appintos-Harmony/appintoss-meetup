---
status: 완료 (Codex 독립검토)
reviewer: Codex
reviewed_branches: [feat/TASK-20260622-029-audio (PR#1), feat/TASK-20260622-031-home-hub (PR#2)]
reviewed_commit: 031 HEAD 9db1041 (029 b21aba8은 031의 조상)
verdict: PR#1=Rework · PR#2=GO
last_updated: 2026-06-22
related: [TASK-20260622-029, TASK-20260622-031, DEBATE-20260622-013, DL-20260622_초기화면_오디오_정합]
---

> 본 검토는 Codex 독립검토로 보강 완료된 REVIEW-032 정본이다.

# REVIEW-20260622-032 · 초기화면/오디오 PR 독립검토

> 검토 방식: Codex가 **실제 커밋 코드·작업지시서·회의록·머지계약 직독**으로 교차검증. 선행 검토 주장 중 실파일과 어긋난 3건을 정정(주의: 표기). 실측: `vitest run` → **24 passed**, `tsc --noEmit` → **exit 0**(031 HEAD 9db1041 재현). `029`는 `031`의 조상 → 두 PR 상호 머지 선형·안전.

## ① PR별 판정
| PR | 브랜치 | 판정 | 사유 |
|---|---|---|---|
| **PR #1** 오디오 | `feat/...-029-audio` | **Rework** | 코드 품질 GO 수준(평균율·보이싱·폴리포니·음색·테스트). 단 **거버넌스 모순**(소유권 문서 불일치, ④) · 정합/조장 승인 전 머지 보류. |
| **PR #2** 홈허브 | `feat/...-031-home-hub` | **GO** | 머지계약 4종·DEBATE-013 수락기준 전부 충족. 차단급 결함 0. 저위험 2건 후속. |

## ② Finding
### HIGH
**H-1. PR#1 스코프 · 소스 문서 자체 모순(거버넌스 차단)** 주의: 페르소나 정정
- 커밋 `b21aba8`이 `routes/Studio.tsx`(+268)·`engine.ts` 음색 2종 포함.
- 페르소나의 "029 forbidden=routes/** 위반"은 **문구상 부정확**(029 forbidden은 api/theme/06_테스트, routes 미포함). 그러나 carve-out 계약(L16 "곽소정=Studio+audio")과 029(`human_owner: 이상혁`, 비목표는 *샘플*만 배제)가 **Studio/음색 소유를 상반 규정** → 문서 불일치 자체가 차단 사유.
- 수정: 조장이 **단일 진실원으로 소유권 확정**: (a) Studio 키보드·음색 2종을 029 정식 산출물로 인정(029 amend + 계약 L16 정합) 또는 (b) 곽소정 TASK-030 재귀속. **의사결정_기록 명시 필요.**

**H-2. clearMelody 일괄 청산 로직 단위테스트 미검증**
- `Studio.tsx:256-268`. 코드 정독상 정확(activeNotes 순회 poly-up + ref 정리)하나 컴포넌트 메서드라 회귀망 0. reducer 테스트는 단일 poly-up만 커버.
- 수정: 청산 핵심을 reducer 헬퍼(`closeAllNotes`)로 추출·순수화 + "동시 2~3음 일괄 off" 테스트.

> 주의: 페르소나의 **030(events.ts/NoteEvent) 런타임 충돌 HIGH 3건은 차단 사유에서 제외.** TASK-030·events.ts는 두 PR 브랜치에 미존재(미래 스트림). → ⑤ 잔여 위험으로 이관.

### MEDIUM
- **M-1.** `melodyDown` 발음이 `ensureAudio().then()` 뒤 → 최초 언락 대기 중 옥타브/모드전환 끼면 무음 미세 race(`Studio.tsx:230-239`, 빈도 낮음). 수정: ready 경로 동기 발음 or 진입 시 선제 언락.
- **M-2.** 수치 드리프트(문서 16/16 vs 실측 24/24) + `평가증빙/테스트증빙_목록.md` EV 미연결. → 정정·연결. *(부분 해소: 중복 029 삭제·평가증빙 추가 커밋 3baa959)*
- **M-3.** Studio 멀티터치 폴리포니 경로 자동 테스트 0(24개 전부 순수모듈). → 순수헬퍼 추출 테스트 or 실기기 QA 증빙.
- **M-4.** 승인 게이트 미완 · 회의록 Draft·HUMAN_APPROVAL 공란, 작업지시서 In Progress, 031 "소급 발행" 자인. → 조장 승인 + status 전이.

### LOW
- **L-1.** 이어하기 칩이 멜로디 개별음명을 회색 칩 표시(`Home.tsx`, graceful fallback 동작 → 차단 아님). `isChordName` 필터/‘멜로디’ 배지 권고.
- **L-2.** `Onboarding.start()` try/finally 없음 → prod `getAnonymousKey` 실패 시 busy 고착. `finally{setBusy(false)}` 보강.
- **L-3.** play() voice split 불변식(`isChordName`+`OCTAVE_MAX=5`) 회귀 테스트 부재.
- **L-4.** 029 `related_requirements: []` 추적 단절.

## ③ 수락기준
- **PR#1:** 평균율·보이싱·폴리포니·stuck방지·voice분리·음색2종 = PASS(코드·실측). clearMelody 동기화 = PASS코드/주의:무테스트. 테스트 24/24·tsc0 = PASS실측이나 청산·voice·멀티터치 **커버리지 불충분**.
- **PR#2:** route home·community append·Studio props 동결·ChordEvent 불변·온보딩→home·2카드·**빈상태 명시**·이어하기 읽기전용·곡4+ 3개·오프라인 안전·닉 게이트 = **전부 PASS**(실파일). 자체 테스트 0 → tsc+수동검토 의존.

## ④ 거버넌스 리스크
1. **소유권 문서 모순(최우선, 조장 결정).** 2. 031 소급 발행. 3. 승인 게이트(회의록 Draft·작업지시서 In Progress). 4. 증빙/수치 드리프트. 5. 029 추적성 공란.
> *부분 해소:* 커밋 `3baa959`로 중복 029·회의록 제거, DL-20260622(소유권 정합 기록)·평가증빙·031 작업지시서 추가.

## ⑤ 잔여 위험 · 머지 전 조건
**잔여(차단 아님):** 미래 TASK-030 통합 시 `events.ts NoteEvent↔ChordEvent` contract-first(계약 §3·§5), L-1 멜로디 칩, Studio의 SessionTrack 인라인 생성.
**머지 전 조건:**
- **PR#2(031): GO**: carve-out §4 순서대로 먼저 머지 가능(후속 L-1/L-2 비차단).
- **PR#1(029): 아래 충족 시 GO**: ① 조장이 Studio/음색 소유권 확정+의사결정 기록(H-1) ② 회의록 HUMAN_APPROVAL+본 REVIEW 기록+status 전이 ③ 테스트증빙 24/24 연결·16/16 정정 ④(강력 권고) clearMelody reducer 추출+테스트(H-2).

**근거 강도:** 코드 정확성·24/24·tsc0 = 실파일/실행(강). 거버넌스 모순 = 문서 직독(강). "030 충돌"·"routes 금지 위반" = 추정/부정확(강등·정정).
