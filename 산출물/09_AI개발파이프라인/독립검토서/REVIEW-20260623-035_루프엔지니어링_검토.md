# REVIEW-20260623-035 독립검토서 · 루프 엔지니어링

```yaml
id: REVIEW-20260623-035
related_task: TASK-20260623-010
review_agent: Claude 적대적 워크플로(4렌즈) — same-family draft. Codex 진짜 교차검토 아님
human_reviewer: 이상혁(대기)
reviewed_commit: a4be0aa
status: Draft
```

## 판정

**Rework**(검토 시점, blocker 다수) → 조치 후 재검증 PASS → 조건부 수락 대기.

> 독립성 한계(정직): 본 검토는 같은 모델 계열(Claude) 워크플로의 적대적 초안이다. CLAUDE.md §6.1.3·§7.4의 "다른 계열 독립 검토"를 **닫지 못한다.** 진짜 교차검토는 사람이 Codex로 별도 구동해야 한다. 단, 발견 사항은 전부 **코드로 실증**했으므로 그 자체로 유효하다.

## 검토 범위
커밋 `a4be0aa`의 자율 엔지니어링 루프(`작업_인덱서`·`코드게이트`·`루프_가드`·`loop-engineer.md`). 4개 적대적 렌즈(게이트 우회·코드게이트 정확성·파서/인덱서·정직성/자기보호), 30개 발견.

## 제공되지 않은 자료
없음(저장소 전체 접근). 단 검토 중 병렬 커밋 `7f3c5fc`(사람이 추가한 `코드검증.mjs`·`검증_전체 --code`)가 드러나, 구현자의 `코드게이트.mjs`와 중복임이 확인됨.

## 발견 사항 (실증 + 조치)

| ID | 심각도 | 내용 | 조치 |
|---|---|---|---|
| B1 | blocker | VERIFY가 `코드게이트 --staged`를 빈 staging에 호출 → `anyCode:false` → tsc/vitest 매 루프 미실행(no-op PASS) | **해소**: 중복인 `코드게이트.mjs` 폐기, 루프는 `검증_전체 --code`(항상 실행) 사용 |
| B2 | blocker | `커밋.mjs`가 보호경로·게이트 결과 미확인 · 권한 상승이 커밋 1회 거리 | **부분 해소**: `커밋.mjs --guard-task <id>` 추가(스테이징 집합에 diffGuard 강제, 위반 시 자동 unstage). 완전 백스톱(pre-commit 훅 연동)은 P2 |
| B3 | blocker | `tooling/scripts`가 보호경로에 없어 루프가 자기 게이트 수정 가능 | **해소**: PROTECTED_PATHS에 `tooling`·`.claude`·`.git`·`.github`·`.gitignore`·`.gitattributes` 추가(디렉터리 전체) |
| B4 | blocker | 대소문자 무시 FS에서 `.CLAUDE/**`가 세 게이트 통과 | **해소**: 공유 `canonicalize()`(소문자·역슬래시·`..` 정규화)로 모든 비교 일원화 |
| B5 | major | 반복 캡이 `--max`로 상향 가능(무상한) | **해소**: 단조 천장 + 절대상한 `ABS_MAX_ITER=20`(올릴 수 없음) |
| B6 | major | 루프가 `검증_전체`를 `--code` 없이 호출(코드검증 미실행) | **해소**: 드라이버가 `검증_전체 --code` 호출(환각 아님 · 병렬 커밋으로 실재) |
| B7 | major | `withinAllowed` 접두 폴백이 형제 디렉터리·깊이 누수(`apps/miniappEVIL`, `src/*`→임의깊이) | **해소**: 세그먼트 경계 앵커, `/*`(한 단계)와 `/**`(임의깊이) 구분 |
| B8 | major | `forbidden_paths` 파싱되나 강제 안 됨 | **해소**: `diffGuard`가 `forbidden_paths` 매치 거부 |
| B9 | major | check-diff(워킹트리)와 commit(부분집합) TOCTOU | **해소**: stage→`check-diff --staged`→`커밋.mjs --guard-task`로 검사=커밋 대상 일치 |
| B10 | major | `packages/**`·`infra/**`·`apps/api` 코드가 게이트 생략 | **해소**: `isUngatedCode()`가 miniapp 외 코드경로를 check-diff에서 STOP(사람 검증) |
| B11 | major | `touchesHumanApproval`이 신규생성·END-only 마커 미탐 | **해소**: HEAD+워킹 양쪽, START·END 둘 다 검사 |
| 기타 | minor/nit | `..` 미정규화, 중복키 last-wins, `.gitignore` 비보호, quotepath | **해소**: canonicalize·게이팅키 중복 fail-closed·보호경로 포함·git 헬퍼 quotepath |

오탐(리뷰어 §4 자인): 순진한 과넓은 글롭(`**`·`*`·trailing-slash) 우회는 원래 막혔음. `apps/api` HARD-STOP 로직 자체는 정상(발동만 B1로 깨졌던 것). NFD 유니코드 우회는 저확신(환경상 비재현).

## 실행 또는 확인한 명령
```
node tooling/scripts/루프엔진_검증.mjs      → 16/16 PASS (회귀 포함)
node tooling/scripts/검증_전체.mjs --code   → docs·links·secrets·tsc·vitest 58/58 PASS (exit 0)
익스플로잇 재확인 스크립트                   → B3·B4·B7·traversal·.gitignore 전부 닫힘
```

## 남은 위험
- **B2 완전 백스톱 미완**: `--guard-task`는 루프가 그 옵션으로 커밋할 때만 강제. LLM이 옵션을 빠뜨리는 경로를 코드로 못 막음 → pre-commit 훅을 루프 상태에 연동하는 P2 필요.
- **독립성**: 본 검토는 same-family. 진짜 Codex 교차검토 미수행.
- apps/api 자동 게이트 부재는 설계상 사람 검증으로 위임(테스트 프로젝트 추가 전까지).

## 사람 승인 질문
1. B2 P2(pre-commit 훅 연동)를 무인 cron 승급 전 선결 조건으로 둘지.
2. Codex 진짜 교차검토를 언제 구동할지.
3. `코드검증.mjs`(사람)와 본 루프의 게이트 통합 방향 확인(현재 루프는 `검증_전체 --code` 단일 사용).
