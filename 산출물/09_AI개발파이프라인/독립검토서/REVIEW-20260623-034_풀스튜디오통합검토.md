```yaml
id: REVIEW-20260623-034
related_task: TASK-20260623-005
review_agent: Claude Code (자동 워크플로우 에이전트 — 교차계열 아님)
human_reviewer: (검토 대기)
reviewed_commit: 6372dc3 (apps diff), review/fullstudio HEAD 219b6ac 확인
status: Draft
```

# REVIEW-20260623-034 풀스튜디오 통합 검토 (Claude 자체 — Codex 아님)

> ⚠️ **독립성 한계(정직 고지).** 이 검토는 Claude Code 워크플로우 에이전트가 수행했다. 교차계열(Codex) 독립검토가 아니므로 CLAUDE.md §6.1 독립성 요건을 **충족하지 않는다.** 아래 발견 사항은 실제 코드 근거가 있어 유효하지만, 정식 독립검토는 Codex로 별도 수행해야 한다(프롬프트는 팀에 전달됨). 처음 frontmatter가 `review_agent: Codex`로 잘못 표기되어 정정했다.

## 판정

**Accept with Conditions / Conditional GO**

단위테스트 58/58, 웹 빌드, 로컬 API smoke, 하네스 기본 검증은 모두 PASS다. 다만 공유 세션 수신 후 첫 트랙이 `sessionTracks`와 현재 take에 동시에 들어가 합주 재생·재공유 시 중복될 수 있고, 합주 재생 보이스가 라우트 이탈 시 정리되지 않는 경로가 있다. 이 두 건은 풀스튜디오 데모 핵심 동선과 직접 맞닿아 있어 머지·발표 전 수정 조건으로 둔다.

## 검토 범위

- 대상 브랜치: `review/fullstudio`
- 대상 diff: `git diff 31d7e15~1..6372dc3 -- apps/`
- 핵심 파일:
  - `apps/miniapp/src/routes/Studio.tsx`
  - `apps/miniapp/src/components/studio/NoteEditor.tsx`
  - `apps/miniapp/src/audio/chordReducer.ts`
  - `apps/miniapp/src/lib/loop.ts`
  - `apps/miniapp/src/lib/storage.ts`
  - `apps/miniapp/src/lib/share.ts`
  - `apps/api/server.mjs`

## 제공되지 않은 자료

- 실제 토스 WebView 실기기 녹음·제스처·오디오 청취 증거: **Not Available**
- 2대 기기 동시 공유/폴링 영상 또는 로그: **Not Available**
- 브라우저 UI 조작 자동화 결과: **Not Run**
- 운영 AWS 서버 live smoke: **Not Run**. 로컬 임시 DB smoke만 수행.

## 수락 기준별 결과

| 기준 | 결과 | 근거 |
|---|---|---|
| 정확성·회귀 | Conditional | 58/58 PASS지만 공유 세션 중복 적재와 합주 보이스 정리 누락이 있음. `Studio.tsx:229-238`, `:806-814`, `:698-719`, `:183-200`. |
| loopEvents 타일링 경계 | Partial | 순수 테스트 6개 PASS. 다만 열린 노트를 `loopTicks - 1`에서 닫아 루프 경계에 1틱 공백이 생길 수 있음. `loop.ts:19`, `loop.test.ts:24-30`. |
| 플레이헤드 타이밍 | Partial | rAF 기반 빨간 재생선은 구현됨. 단 중간 위치에서 재생하면 이미 켜져 있어야 할 노트를 재발음하지 않음. `NoteEditor.tsx:132-144`, `:150-168`. |
| createVoice dispose | Conditional | 일반 단일 take는 dispose 예약 있음. 합주 재생은 unmount cleanup에서 `stopJam()`이 빠져 지속음/노드 잔류 가능. `Studio.tsx:183-200`, `:691-719`. |
| 보안·입력검증 | Partial | SQL은 prepared statement라 주입 위험은 낮음. payload 1MB 제한은 있음. 그러나 `events` 원소, `instrument`, `style`, `owner`, `bpm` 런타임 검증이 부족함. `server.mjs:40-49`, `:67-101`. |
| 테스트 커버리지 | Partial | `npm --prefix apps/miniapp test` 7파일 58테스트 PASS. WebAudio/UI/multitrack 수신 중복/API negative 검증은 없음. |
| 아키텍처·문서 동기화 | Partial | 시스템 구성도·테이블 정의서·테스트 결과서는 반영됨. 별도 `API_명세서.md`는 Not Started이고 PRD의 입력검증 주장과 실제 서버 검증 수준이 어긋남. `산출물_목록.md:54`, `:64`, `PRD.md:83`. |

## 발견 사항

| ID | 심각도 | 내용 | 근거·재현 | 조치 |
|---|---|---|---|---|
| R34-001 | P1 | 공유 세션을 받으면 첫 트랙이 `sessionTracks`와 현재 take에 동시에 들어가 합주 재생·저장·공유 때 중복될 수 있다. | `forked` 처리에서 `stateRef.current.events = base.events`와 `setSessionTracks(forked.tracks)`를 같이 수행한다(`Studio.tsx:229-238`). `confirmReceive`도 동일하다(`:806-814`). 이후 `playSession`은 `sessionTracks` 전부에 현재 take를 다시 push한다(`:702-705`). | 수신 세션은 `sessionTracks=pending.tracks`, 현재 take는 빈 상태로 시작하거나, 반대로 현재 take로 복사할 경우 `sessionTracks`에서 base를 제외한다. 2트랙 세션 수신→합주 재생→공유 재조회 회귀 테스트 추가. |
| R34-002 | P1 | 합주 재생 중 라우트 이탈/언마운트 시 `voicesRef` 보이스가 dispose되지 않을 수 있다. | unmount cleanup은 `stopPlayback()`·camera 정리만 호출한다(`Studio.tsx:183-200`). 합주 보이스는 `playSession`에서 `voicesRef`에 저장되고 `stopJam()`에서만 dispose된다(`:691-719`). `stopPlayback()`은 `playTimersRef`만 비우므로 이미 울린 sustained note의 off timer가 사라지고 보이스는 남을 수 있다. | unmount cleanup에 `stopJam()` 추가. `stopJam()`이 jam timer까지 명확히 정리하도록 timer bucket을 분리하거나 `playTimersRef` 정리 책임을 통합한다. |
| R34-003 | P1 | API가 `events` 원소와 `instrument/style`을 검증하지 않아 악성/오염 세션이 클라이언트 재생 경로를 깨뜨릴 수 있다. | 서버는 `Array.isArray(events)`만 확인하고 JSON 문자열로 저장한다(`server.mjs:97-101`). 클라이언트 정규화는 `e.chord.startsWith()` 전제를 둔다(`events.ts:51-65`). PRD는 events·instrument·style·owner 스키마 제한을 주장한다(`제품요구사항정의서_PRD.md:83`). | 서버에 schema guard 추가: tick finite range, phase on/off, chord string length/prefix, source whitelist, instrument/style whitelist, owner/name length, event count 제한. 잘못된 payload는 400. |
| R34-004 | P2 | NoteEditor에서 빨간 재생선을 노트 중간에 두고 실행하면 해당 시점에 이미 울려야 할 노트가 소리 나지 않는다. | `playFrom`은 `ms < fromMs - 1` 이벤트를 모두 건너뛰고 이후 이벤트만 예약한다(`NoteEditor.tsx:132-144`). active 하이라이트는 playhead 구간을 기준으로 켜진다(`:303-308`)라서 화면은 켜진 노트처럼 보이나 오디오는 silent일 수 있다. | fromTick을 가로지르는 노트는 즉시 `on`을 합성하고, 이후 off를 예약한다. 코드/멜로디/드럼별 테스트 또는 수동 QA 추가. |
| R34-005 | P2 | 구간 반복에서 열린 노트를 `loopTicks - 1`에 닫아 마디 끝까지 지속되는 음이 1틱 짧아질 수 있다. | `loop.ts:19`가 synthetic off tick을 `loopTicks - 1`로 만든다. 테스트도 16틱 루프에서 15, 31 off를 기대한다(`loop.test.ts:24-30`). | 의도라면 문서화. 자연 sustain이 목표라면 off를 `loopTicks`로 두고 같은 tick off→on 정렬을 보장한다. 경계 sustain 회귀 테스트 추가. |
| R34-006 | P2 | 네트워크 세션에서 레이어 추가 실패를 조용히 삼켜 로컬 UI와 서버 상태가 갈라질 수 있다. | `addLayer`는 로컬 `setSessionTracks`를 먼저 수행하고, `addTrack(...).catch(() => {})`로 실패를 무시한다(`Studio.tsx:821-829`). | 네트워크 실패 시 toast와 재시도/rollback. 최소한 `sessionCode`가 있으면 업로드 성공 후 로컬 반영 또는 pending 표시. |
| R34-007 | P3 | 음 편집 미리듣기는 트랙별 악기와 무관하게 비드럼을 항상 피아노로 재생한다. | `NoteEditor`는 `createVoice('piano','grand')`와 `createVoice('drum','analog')`만 생성한다(`NoteEditor.tsx:124-144`). | NoteEditor에 instrument/style props를 전달해 현재 take의 악기로 미리듣기. 제품 품질 개선 항목. |
| R34-008 | P3 | API 명세서가 별도 산출물로 아직 없다. | `산출물_목록.md:54`는 `API_명세서.md`를 Not Started로 표기하고 `:64`에서 분리 작업이 남았다고 쓴다. | 4엔드포인트 요청/응답/오류/검증 규칙을 별도 API 명세서로 분리. R34-003과 함께 갱신. |

## 실행 또는 확인한 명령

```text
git branch --show-current
→ review/fullstudio
```

```text
git diff --stat 31d7e15~1..6372dc3 -- apps/
→ 7 files changed, 374 insertions(+), 100 deletions(-)
```

```text
npm --prefix apps/miniapp test
→ Test Files 7 passed (7)
→ Tests 58 passed (58)
```

```text
npm --prefix apps/miniapp run build:web
→ tsc --noEmit && vite build
→ ✓ built in 1.76s
→ warning: index JS 598.90 kB, chunk larger than 500 kB
```

```text
API smoke (임시 PORT=18080, 임시 SQLite DB)
→ {"health":true,"code":"WJ2Q7M","trackId":2,"trackCount":2,"firstInstrument":"guitar","secondInstrument":"drum"}
```

```text
node tooling/scripts/검증_전체.mjs
→ 전체 결과: PASS — 하네스 기본 검증
→ 산출물 존재: 존재 42 / 누락 7 / 전체 49
```

## 남은 위험

- 실기기 WebView의 AudioContext unlock, 카메라 권한, 멀티터치, 샘플 CDN/CSP는 Not Run.
- 2대 기기 polling으로 실제 `addTrack` 전파를 확인하지 못했다.
- Tone.js 보이스/샘플 노드 메모리 프로파일은 Not Run.
- API는 데모용 무인증 구조라 공개 운영 전 rate limit, schema validation, 삭제/신고 운영 정책이 필요하다.

## 사람 승인 질문

1. 공유 세션 수신 시 첫 트랙을 “편집 가능한 복사본”으로 둘지, “배경 레이어”로만 둘지 결정이 필요하다. 권장: 배경 레이어로만 두고 현재 take는 비워 시작.
2. R34-001~003을 머지 전 필수 수정 조건으로 둘지 승인 필요. 권장: 승인.
3. 실기기 2대 데모를 발표 전 필수 검수로 둘지 승인 필요. 권장: 승인.
