---
status: Draft
owner: 양록빈
reviewers: [Claude Code]
last_updated: 2026-06-24
---

# REVIEW-20260624-041 독립검토서 — TASK-040 UI 다듬기 + 법적고지 모달

```yaml
id: REVIEW-20260624-041
related_task: TASK-20260624-040
review_agent: Claude Code
human_reviewer: 양록빈(QA) · 이상혁(머지 승인)
reviewed_commit: 85146e5 (base develop 6bfd97f)
status: Draft
```

> 구현자=김민혁(커밋 계정 lonis <ruinloni@gmail.com>), 검토자=Claude Code → 구현자≠검토자(§7.4 충족). 기존 `REVIEW-20260624-040`은 TASK-009 보안 라운드3 검토물이라 번호 충돌 회피로 본 검토서는 **041**.

## 판정

**Accept with Conditions (Conditional Go).** 게이트 실측 PASS·회귀 위험 낮음. develop 머지를 막는 건 코드가 아니라 절차(작업지시서·독립검토서·author 정합)였고, 본 검토서 + [TASK-040 작업지시서](../작업지시서/TASK-20260624-040_UI다듬기_김민혁.md) 생성 + author 정합(lonis=김민혁)으로 해소된다. 남은 조건은 조장 승인 + `--strict`.

## 검토 범위

브랜치 `feat/TASK-20260624-040-ui-polish`, `develop...85146e5` diff 18파일 +387/−94. 격리 git worktree에서 게이트 실행, `legal.ts` 전체 정독, `manifest`/diff 확인.

## 제공되지 않은 자료 (Not Run / Unverified)

- 실기기 시각 회귀(회전·바텀시트 z-index 가림) — **Not Run**.
- 토스 WebView에서 `manifest.webmanifest` 실효 — **Unverified**.

## 수락 기준별 결과

| 기준 | 결과 | 근거 |
|---|---|---|
| `tsc --noEmit` | PASS | 0건 (worktree 실행) |
| `vitest run` | PASS | 58/58 |
| `vite build` | PASS | 646.30kB (gzip 187.03), 1.75s |
| 도메인/API/테이블 무변경 | PASS | diff = CSS·뷰 레이어 한정 |
| 법적고지 정직 표기 | PASS | `legal.ts` 헤더 '초안·법무 검토 필요' |

## 발견 사항

| ID | 심각도 | 내용 | 근거·재현 | 조치 |
|---|---|---|---|---|
| F1 | P2 | 커뮤니티 페이저·정렬 토글이 `stopPreview()` 미호출 → 미리듣기 재생 중 페이지/정렬 전환 시 예약 노트 지속·정지 UI 사라짐 | `Community.tsx:544,546,416,417` (다른 이탈경로 :74,188,256,378은 호출) | onClick에 `stopPreview()` 추가(1줄) 또는 active-not-in-page effect |
| F2 | P1 | 개인정보 처리방침 PIPA §30 필수기재(보호책임자·항목별 보유기간·파기절차·권리행사·만14세) 누락/추상 + 운영주체(B-01 Open) 공백 | `legal.ts` PRIVACY | 출시 전 법무 보완(머지 차단 아님). 발표·종료보고서·출시점검표에 '초안(미완)'으로 단계 분리 |
| F3 | P2 | 드럼 landscape 4열 그리드·`z-index isolation` = 레이아웃 구조 변경 경계선(§17 동결) | `DrumPad.tsx`·`theme.css` | 실기기검수표에 시각 회귀(회전·바텀시트) RT 추가 후 육안 |
| F4 | P3 | 번들 JS 646KB(+47)·코드분할 없음·`logo.png` 357KB 과대·manifest 실효 불명 | `vite build`·`public/` | 후속 chore(코드분할·로고 재인코딩) |

> F1~F4 중 머지 차단급(P0/P1 회귀) 없음. F2는 '출시' 선결이지 'develop 머지' 차단 아님.

## 실행 또는 확인한 명령

- `git worktree add --detach … 85146e5` → `node tooling/scripts/코드검증.mjs` → **PASS**(tsc 0·vitest 58/58)
- `cd apps/miniapp && npx vite build` → **646.30kB**
- `git show 85146e5:apps/miniapp/src/lib/legal.ts` (정독)
- `git log`/`grep` — 작성자 lonis=김민혁 확인, TASK-040 작업지시서 부재 확인(본 검토 시 생성)

## 남은 위험

- **절차:** author 정합(해소: lonis=김민혁), 작업지시서(TASK-040 생성), 독립검토서(본 문서) → DL-014 남은 조건은 **조장 승인 + `--strict`**.
- **제품:** F1 데모 동선 영향은 작으나 후속, F2 출시 선결, F3 시각 회귀 미검증.

## 사람 승인 질문

1. (이상혁) F1 `stopPreview` 1줄 수정을 **머지 전 포함**할지, **후속(P2)**으로 둘지?
2. (양록빈) 시각 회귀 RT(회전·바텀시트) 추가 후 육안 검수 일정?
3. (이상혁) DL-014 3조건(승인 + `--strict` + 검토서) 충족으로 **develop 머지 진행**? → 단 `--strict`는 현재 FAIL(아래 ④ 참조).

## ④ 검증_전체 --strict 결과 (2026-06-24 실행)

`node tooling/scripts/검증_전체.mjs --strict` → **FAIL (2건)**. 단 **이 브랜치와 무관한 저장소 전역 결손**이다:

- **산출물 존재(엄격) FAIL** — 필수 6종 미존재: 종료보고서, 프로그램 설계서, 피그마 디자인인계서, API 명세서, 권한 설계서, ADR 폴더(`산출물/04_시스템설계/아키텍처_의사결정기록`).
- **내부 링크 FAIL** — `산출물/03_화면설계/하모니_피그마_화면명세서.md` 2건(`Button weak/small`, `Button 주 CTA/small`).
- **PASS:** 팀공유문서(엄격)·작업지시서·비밀값.

→ **DL-014 ④ 조건 미충족.** TASK-040 자체는 게이트(tsc·vitest·build) PASS이나, 저장소 strict는 위 결손 해소(특히 종료보고서·프로그램 설계서 생성 + 링크 2건 수정) 전까지 FAIL이므로 **현재 develop 머지 게이트는 열리지 않는다.** 이는 브랜치 품질 문제가 아니라 저장소 산출물 결손(기존 P0)이다.

## F1 수정 제안 (패치 — 미적용, 이상혁 결정)

미리듣기 정지 누락(F1)의 최소 수정. `apps/miniapp/src/routes/Community.tsx`의 정렬 토글(416·417)과 페이저(544·546)에 `stopPreview()` 선행 추가:

```diff
-            <button className="seg" data-on={sort === 'recent'} onClick={() => { setSort('recent'); setPage(0); }}>최신순</button>
-            <button className="seg" data-on={sort === 'popular'} onClick={() => { setSort('popular'); setPage(0); }}>인기순</button>
+            <button className="seg" data-on={sort === 'recent'} onClick={() => { stopPreview(); setSort('recent'); setPage(0); }}>최신순</button>
+            <button className="seg" data-on={sort === 'popular'} onClick={() => { stopPreview(); setSort('popular'); setPage(0); }}>인기순</button>
@@ 페이저 @@
-            <button className="chip chip-ghost" disabled={safePage <= 0} aria-label="이전 페이지" onClick={() => setPage(safePage - 1)}>‹ 이전</button>
-            <button className="chip chip-ghost" disabled={safePage >= totalPages - 1} aria-label="다음 페이지" onClick={() => setPage(safePage + 1)}>다음 ›</button>
+            <button className="chip chip-ghost" disabled={safePage <= 0} aria-label="이전 페이지" onClick={() => { stopPreview(); setPage(safePage - 1); }}>‹ 이전</button>
+            <button className="chip chip-ghost" disabled={safePage >= totalPages - 1} aria-label="다음 페이지" onClick={() => { stopPreview(); setPage(safePage + 1); }}>다음 ›</button>
```

대안: `active`가 현재 페이지 `items`에 없으면 정지하는 effect 1개로 통합(이탈 경로별 누락을 구조적으로 제거). **본 패치는 미적용** — 머지 전 포함 여부는 이상혁 결정, 반영은 작성자 김민혁이 브랜치에서 수행.
