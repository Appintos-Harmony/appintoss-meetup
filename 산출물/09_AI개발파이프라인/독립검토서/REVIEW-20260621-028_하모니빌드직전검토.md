# REVIEW-20260621-028 독립검토서

```yaml
id: REVIEW-20260621-028
related_task: TASK-20260621-027
review_agent: Codex
human_reviewer: 이상혁
reviewed_commit: 5fb1dbf
status: Final
```

## 판정

Rework

빌드 직전 기준으로는 **하모니 MVP v1-final 빌드 GO 불가**다. 문서상 DL-019는 라운드3 스펙을 승인했지만, 현재 `apps/miniapp`는 3라우트 앱이 아니라 `index.html`에서 `gesture-spike.html`로 리다이렉트하는 스파이크 상태다. 또한 MVP v1-final 안에서도 라운드3이 위 P0보다 우선이라고 쓰였지만, 아래 원안 섹션에는 다크 패널·4라우트·드럼/기타/보관함 등 폐기된 내용이 그대로 남아 구현자가 잘못 집을 여지가 있다.

빌드 전 최소 조건은 ① 스펙 충돌 정리 ② activeChord reducer 구현 기준 확정 ③ Phase 0/1 순서 고정 ④ 백엔드 컷라인을 더 이른 판정 단위로 쪼개기 ⑤ appName/경로 이동 사이드이펙트 정리다.

## 검토 범위

- 브랜치: `feat/TASK-20260621-027-gesture-spike`
- 기준 커밋: `5fb1dbf`
- 저장소 루트: `C:\Users\DEV-PC\Desktop\appintoss-meetup`
- 검토 대상:
  - `산출물/03_화면설계/하모니_화면명세서.md`
  - `산출물/00_프로젝트관리/의사결정_기록.md` DL-019, DL-018, DL-016
  - `산출물/09_AI개발파이프라인/토론기록/DEBATE-20260621-011_하모니설계회의.md`
  - `apps/miniapp/granite.config.ts`
  - `apps/miniapp/public/gesture-spike.html`
  - 보조: `apps/miniapp/index.html`, `apps/miniapp/package.json`

## 제공되지 않은 자료

- AWS Ubuntu 서버 접속 정보/배포 로그/도메인·TLS 상태: Not Available.
- 실제 2장비 polling 테스트 로그: Not Run.
- build 출력: Not Run. 빌드 산출물 생성이 생길 수 있어 독립 문서 검토 범위에서는 실행하지 않았다.
- 앱인토스 검토 요청/출시 결과: Not Run.

## 수락 기준별 결과

| 기준 | 결과 | 근거 |
|---|---|---|
| 1. MVP v1-final이 18h에 구현 가능한지 | Fail | 라운드3은 3라우트+온보딩, 음색 바텀시트, 클립보드 자동 인식, AWS 3엔드포인트, polling, reducer, 라이트 테마를 동시에 요구한다(`하모니_화면명세서.md:31-40`, `의사결정_기록.md:128-137`). 현재 앱은 `index.html`에서 스파이크로 redirect만 한다(`apps/miniapp/index.html:9-10`). |
| 1-a. 빠진 화면 상태·엣지 | Partial | 필수 상태 목록은 있으나(`하모니_화면명세서.md:26`) 라우트별 행동 정의가 부족하다. AudioContext 잠금, polling 실패, 미저장 이탈, 클립보드 비허용, 받은곡 버전 불일치가 어떤 CTA/복구 흐름으로 이어지는지 없다. |
| 2. activeChord reducer + 제스처/터치 동시 바인딩 | Fail | DL-019는 activeChord 변경만 기록한다고 정했지만(`의사결정_기록.md:134`), 스파이크 코드는 `curZone/playing` 직접 상태와 `performance.now()` 기반 이벤트 기록이다(`gesture-spike.html:88-100`, `:153-170`). touch source, 200ms 재개, tick quantize 구현 근거가 없다. |
| 3. 백엔드 컷라인 6/10/14h 현실성 | Partial | 3엔드포인트+SQLite+polling 자체는 18h 조건부 가능 범위이나, HTTPS/도메인/CORS/프로세스 관리/스키마 검증/세션 만료/이벤트 크기 제한이 컷라인에 없다. 6h에 HTTPS만 처음 판정하면 너무 늦다(`하모니_화면명세서.md:36`, `의사결정_기록.md:133`). |
| 4. Phase 0/1 착수 순서·우선순위 | Fail | 문서에는 결과 스펙은 있으나 "라이트 TDS 셸 + 3라우트 + 온보딩 → 스튜디오"의 구현 순서가 acceptance 단위로 쪼개져 있지 않다. 현 앱은 라우트도 React entry도 없이 스파이크 redirect다(`apps/miniapp/index.html:9-10`). |
| 5. ASCII 경로 이동 사이드이펙트 | Partial | `git rev-parse --show-toplevel`은 ASCII 루트를 반환하고 `core.hooksPath`는 `tooling/git-hooks`라 git 훅은 상대경로로 안전하다. 그러나 문서에는 예전 한글 루트가 남아 있다(`의사결정_기록.md:27`, `프로젝트_헌장.md:17`, 기존 REVIEW 다수). 앱 설정도 아직 `meetup-lite`다(`granite.config.ts:4,8`). |

## 발견 사항

| ID | 심각도 | 내용 | 근거·재현 | 조치 |
|---|---|---|---|---|
| H28-001 | P0 | 빌드 대상이 MVP v1-final 앱이 아니라 스파이크 redirect다. 이 상태로 빌드하면 "3라우트+온보딩+스튜디오"가 아니라 `gesture-spike.html`만 실행된다. | `apps/miniapp/index.html:9-10`은 스파이크로 redirect. `package.json:6-9`은 build/deploy 스크립트만 있고 앱 소스 entry가 없다. | build 전 최소 React/Vite entry 또는 Granite 앱 entry를 만들고 온보딩·홈·스튜디오·설정 라우트 shell을 먼저 구현한다. |
| H28-002 | P0 | MVP v1-final 문서 내부 충돌이 남아 있다. 라운드3은 100% 라이트와 3라우트를 확정했지만, 아래 원안은 제한적 다크 패널·다크 무대·4라우트/보관함·드럼/기타/베이스를 계속 노출한다. | 라운드3 우선 `하모니_화면명세서.md:31-40`; 충돌되는 이전 P0/원안 `:21`, `:24`, `:45`, `:54`, `:56-65`. | 빌드 지시서에는 라운드3만 남기거나 원안 섹션을 `Superseded`로 접어 구현 금지 표시한다. |
| H28-003 | P1 | activeChord reducer는 문서 결정일 뿐 코드 근거가 없다. 현재 스파이크는 제스처 단일 소스이며 벽시계 ms 이벤트라 2장비 sync와 충돌한다. | tick 계약 `하모니_화면명세서.md:27`; reducer 결정 `:37`; 현재 코드 `gesture-spike.html:94,99,158,164-170`은 `performance.now()`/`setTimeout(ev.t)` 기반. | reducer를 `activeChord, activeSource, tick, phase` 중심 순수 함수로 먼저 만들고 단위 테스트를 붙인다. gesture/touch는 reducer 입력 이벤트만 발행하게 한다. |
| H28-004 | P1 | 제스처 기본+터치 폴백 설계가 "세그먼트 컨트롤"과 충돌할 수 있다. 세그먼트가 모드 선택인지 폴백 전환인지 애매하면 사용자가 두 모드를 동시에 기대한다. | `하모니_화면명세서.md:33`은 제스처 기본+터치 폴백이면서 세그먼트 컨트롤 `[제스처로 반주 | 터치로 멜로디]`를 둔다. DEBATE는 모드 분리를 말한다(`DEBATE...md:26`, `:34`, `:59`). | 세그먼트는 "입력 모드"가 아니라 "현재 입력 상태/전환"으로 정의한다. 카메라 정상 시 제스처 탭 기본, 카메라 실패 시 터치 탭 자동 전환. 동시 바인딩 금지. |
| H28-005 | P1 | 백엔드 컷라인이 너무 늦다. 6h에 HTTPS만 실패 판정하면 이미 프론트 연동 시간을 잡아먹는다. | 컷라인 `하모니_화면명세서.md:36`, `의사결정_기록.md:133`. | T+2h: 도메인/HTTPS/healthz, T+4h: SQLite CRUD, T+6h: POST/GET sessions, T+8h: POST tracks, T+10h: 프론트 polling 연동, T+12h: 2장비 데모. 실패 시 즉시 프리로드. |
| H28-006 | P1 | 클립보드 자동 인식은 데모 리스크가 크다. 권한/브라우저 정책/사용자 제스처 조건이 빠져 있다. | 합주 받기 `하모니_화면명세서.md:35`, DL-019 `의사결정_기록.md:132`. | MVP에는 자동 인식 실패 시 수동 붙여넣기 입력을 숨은 fallback으로 둔다. "코드 타이핑 X"는 발표 UX 목표로만 둔다. |
| H28-007 | P2 | AudioContext 잠금은 상태로만 적혀 있고 실제 재시도/복구 UX가 없다. 스파이크는 시작 버튼에서 `Tone.start()` 실패를 catch하지만 앱 스튜디오 흐름으로 일반화되어 있지 않다. | 필수 상태 `하모니_화면명세서.md:26`; 스파이크 `gesture-spike.html:130-149`. | 첫 소리 1초 목표를 위해 온보딩 완료 후 "소리 켜기" 명시 버튼을 두고 실패 시 터치 재시도 CTA를 고정한다. |
| H28-008 | P2 | 카메라 실패 상태는 적혀 있으나 gesture 루프 정리/권한 재요청/저성능 fallback 기준이 부족하다. | 스파이크 오류 안내 `gesture-spike.html:146-149`; 상태 목록 `하모니_화면명세서.md:26`; 제스처 판정은 y좌표 간이 로직 `gesture-spike.html:79-86`. | 권한 거부, 손 0개 3초, FPS 18 이하 3초, 다수손 감지 각각 터치 폴백 전환 기준을 명시한다. |
| H28-009 | P2 | 100% 라이트 결정과 스파이크 UI가 전면 다크다. 스파이크는 기술 검증으로 남기고 제품 UI로 그대로 빌드하면 DL-019 위반이다. | 100% 라이트 `하모니_화면명세서.md:38`, `의사결정_기록.md:135`; 스파이크 CSS `gesture-spike.html:9-13`은 다크 배경. | 스파이크 코드를 제품으로 직접 빌드하지 말고, 로직만 이식한다. UI는 라이트 shell과 흑건 색상만 사용한다. |
| H28-010 | P2 | appName과 브랜드 상태가 피벗 완료 상태가 아니다. 하모니 빌드라고 해도 콘솔 등록 앱은 `meetup-lite`다. | `granite.config.ts:4,8`은 테스트에서 `meetup-lite` 재사용, DL-018은 appName `harmony` 제안(`의사결정_기록.md:120`). | 빌드가 실기기 스파이크 검증용인지 하모니 제출 후보인지 라벨을 분리한다. 제출 후보면 appName/Gate 0 재실행이 선행되어야 한다. |
| H28-011 | P3 | ASCII 이동은 헤더 버그 회피에는 맞지만, 이전 절대경로 문서가 계속 남아 세션/평가 보고 혼선을 만든다. | `rg` 결과: `의사결정_기록.md:27`, `프로젝트_헌장.md:17`, 기존 REVIEW 다수에 `Desktop\프로젝트\appintoss-meetup`. `git rev-parse --show-toplevel`은 `C:/Users/DEV-PC/Desktop/appintoss-meetup`. | 과거 REVIEW 원문은 보존하되, 현재 상태 문서/DL에 "현 루트=ASCII 경로"를 추가한다. |

## 빌드 전 컷라인 제안

| 시점 | GO 조건 | 실패 시 |
|---|---|---|
| T+1h | 앱 entry가 스파이크 redirect가 아니라 3라우트 shell로 뜸 | 빌드 중지, shell만 완성 |
| T+2h | 온보딩 후 스튜디오에서 터치로 첫 소리 1초 내 발생 | 제스처 통합 금지, 터치+녹음부터 |
| T+4h | activeChord reducer 단위 테스트 통과, tick 이벤트 기록 | 공유/백엔드 착수 금지 |
| T+6h | 제스처가 reducer 입력으로 붙고 터치 폴백 전환 기준 작동 | 제스처는 영상/스파이크 데모로 분리 |
| T+8h | 로컬 녹음·재생·뮤트·삭제·재진입 복구 | 백엔드 공유 컷 |
| T+10h | HTTPS healthz + SQLite CRUD + sessions GET/POST | 프리로드 회귀 |
| T+12h | POST tracks + polling + 2장비 조회 | 공유 데모 컷 |
| T+14h | 2장비 저장·조회·재생 안정 | 프리로드 트랙만 발표 |

## Phase 0/1 우선순위

1. **Phase 0 — Shell 고정:** 라이트 테마, viewport, appName 라벨, 3라우트+온보딩 shell, 토스 뒤로가기 중복 금지.
2. **Phase 1 — 첫 소리:** getAnonymousKey mock/fallback, 닉네임, AudioContext unlock, 터치 4코드 패드, activeChord reducer.
3. **Phase 2 — 스튜디오:** 카운트인, tick clock, 이벤트 녹음/재생, 로컬 저장/복구.
4. **Phase 3 — 제스처 이식:** 스파이크에서 MediaPipe 루프만 가져오고 UI는 라이트 shell 유지. 저성능/권한 fallback 기준 구현.
5. **Phase 4 — 조건부 공유:** AWS 3엔드포인트 + polling. 실패하면 프리로드.

## 실행 또는 확인한 명령

### `git branch --show-current`

```text
feat/TASK-20260621-027-gesture-spike
```

### `git status --short`

```text
?? apps/miniapp/.gitignore
?? apps/miniapp/granite.config.ts
?? apps/miniapp/index.html
?? apps/miniapp/package-lock.json
?? apps/miniapp/package.json
?? 발표자료/발표_스토리보드.md
?? 산출물/09_AI개발파이프라인/독립검토서/REVIEW-20260621-027_하모니제스처스파이크검토.md
```

### `node tooling/scripts/검증_전체.mjs`

```text
# 검증 전체 — 하네스 기본 검증

===== 팀 공유 문서 (팀공유문서_검증.mjs) =====
# 팀 공유 문서 검증 (--allow-template)
검사 대상: 6개 문서

결과: PASS

===== 산출물 존재(보고) (산출물_검증.mjs) =====
# 산출물 검증 (CLAUDE.md §9)
항목(M10 제외): 존재 28 / 누락 21 / 전체 49
M10 프로그램 소스코드: 부분(apps,infra)

누락:
 - M03 종료보고서 → 산출물/00_프로젝트관리/프로젝트_종료보고서.md
 - M04 프로그램 목록 → 산출물/05_개발설계/프로그램_목록.md
 - M06 화면설계서 → 산출물/03_화면설계/화면설계서.md
 - M07 시스템 구성도 → 산출물/04_시스템설계/시스템_구성도.md
 - M08 테이블 정의서 → 산출물/04_시스템설계/테이블_정의서.md
 - M09 프로그램 설계서 → 산출물/05_개발설계/프로그램_설계서.md
 - M11 단위테스트 계획서 → 산출물/06_테스트/단위테스트_계획서.md
 - M12 단위테스트 결과서 → 산출물/06_테스트/단위테스트_결과서.md
 - M13 통합테스트 계획서 → 산출물/06_테스트/통합테스트_계획서.md
 - M14 통합테스트 결과서 → 산출물/06_테스트/통합테스트_결과서.md
 - M15 사용자 매뉴얼 → 산출물/08_사용자매뉴얼/사용자_매뉴얼.md
 - A11 피그마 디자인인계서 → 산출물/03_화면설계/피그마_디자인인계서.md
 - A12 API 명세서 → 산출물/04_시스템설계/API_명세서.md
 - A13 권한 설계서 → 산출물/04_시스템설계/권한_설계서.md
 - A14 ADR(폴더) → 산출물/04_시스템설계/아키텍처_의사결정기록
 - A15 테스트 종합보고서 → 산출물/06_테스트/테스트_종합보고서.md
 - A16 앱인토스 실기기검수표 → 산출물/06_테스트/앱인토스_실기기검수표.md
 - A17 배포 실행절차서 → 산출물/07_배포운영/배포_실행절차서.md
 - A18 롤백 계획서 → 산출물/07_배포운영/롤백_계획서.md
 - A19 출시 점검표 → 산출물/07_배포운영/출시_점검표.md
 - A20 앱인토스 심사제출서 → 산출물/07_배포운영/앱인토스_심사제출서.md

결과: 보고 완료

===== 내부 링크 (링크_검증.mjs) =====
# 링크 검증 (내부 상대 링크)
Markdown 83개, 내부 링크 107개 검사

결과: PASS

===== 작업지시서 (작업지시서_검증.mjs) =====
# 작업지시서 검증
검사 9개 (템플릿 제외)

결과: PASS

===== 비밀값 (비밀값_스캔.mjs) =====
# 비밀값 스캔
비밀값 패턴 없음
결과: PASS

===== 요약 =====
 PASS  팀 공유 문서
 PASS  산출물 존재(보고)
 PASS  내부 링크
 PASS  작업지시서
 PASS  비밀값

전체 결과: PASS — 하네스 기본 검증
```

### `node tooling/scripts/링크_검증.mjs`

```text
# 링크 검증 (내부 상대 링크)
Markdown 83개, 내부 링크 107개 검사

결과: PASS
```

### ASCII 경로 확인

```text
C:/Users/DEV-PC/Desktop/appintoss-meetup
```

### hooksPath 확인

```text
tooling/git-hooks
```

## 남은 위험

- 현재 작업트리에는 앱 설정 파일과 REVIEW-027이 미추적 상태다. 빌드/커밋 전 명시 path 스테이징이 필요하다.
- `npm run build`는 Not Run이다. 현 상태에서 실행하면 스파이크 redirect 빌드만 확인할 가능성이 높아 MVP 검증으로 부적합하다.
- AWS 서버·도메인·TLS·CORS 상태가 제공되지 않았다.
- DL-018은 여전히 주제 자체가 Proposed다. DL-019는 스펙 승인이지 주제 전환 전체 승인으로 해석하면 안 된다.

## 사람 승인 질문

1. 지금 빌드를 "스파이크 검증 빌드"로만 진행할지, 아니면 MVP 앱 shell 구현 후 빌드할지?
2. DL-019 라운드3만 정본으로 남기고 아래 원안 섹션을 Superseded 처리할지?
3. AWS 공유는 T+10h 실패 시 즉시 프리로드 회귀로 강제할지?
