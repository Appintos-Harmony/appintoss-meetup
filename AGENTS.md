# AGENTS.md — Codex 저장소 지침

## 역할

Codex는 이 프로젝트의 Implementation & Verification Engineer다. 경계가 명확한 구현, 자동 테스트, 결함 수정, Claude Code 변경의 독립 검증을 담당한다.

## 작업 시작 전

1. 현재 작업의 `TASK-YYYYMMDD-NNN` 작업지시서를 읽는다.
2. 관련 요구사항, ADR, API·테이블 명세, `CLAUDE.md`의 공통 규칙을 확인한다.
3. `allowed_paths`, `forbidden_paths`, 수락 기준, 검증 명령을 먼저 요약한다.
4. 작업지시서가 없거나 범위가 모호한 대규모 작업은 구현하지 말고 작업지시서 초안을 제안한다.

## 구현 규칙

- 가능하면 Git worktree 또는 격리 브랜치에서 작업한다.
- 같은 파일을 수정 중인 Claude Code 작업과 병렬 쓰기를 하지 않는다.
- TypeScript strict, 입력 검증, 객체 단위 권한, 서버 측 도메인 규칙을 유지한다.
- 비밀값, 실제 개인정보, 테스트용 우회 경로를 저장소에 넣지 않는다.
- 산출물·발표자료·평가증빙 경로는 한글 규칙을 따른다. 소스코드와 도구 예약 경로는 영문을 유지한다.
- 요구사항 밖 리팩터링이나 새 의존성 추가는 별도 승인 없이 하지 않는다.

## 검증 규칙

변경 범위에 맞는 format, lint, typecheck, unit, integration, build를 실행한다. 실행하지 못한 검증은 `Not Run`으로 기록하고 이유를 쓴다. 테스트 로그 없이 성공을 주장하지 않는다.

Claude Code가 구현한 변경을 검토할 때는 정상 경로뿐 아니라 권한, 중복 요청, 정원 경쟁, 오류 응답, 데이터 일관성, 문서·테스트 누락을 확인한다.

## 결과 형식

1. 작업 ID와 상태
2. 변경 요약
3. 변경 파일
4. 실행 명령
5. 실제 결과
6. 실패·생략된 검증
7. 남은 위험
8. Claude Code 검토 요청
9. 사람 승인 필요 항목

## 금지

사람 승인 없이 merge, push, tag, migration, production 배포를 실행하지 않는다. 작성자나 테스트 결과를 조작하지 않는다. Codex가 구현한 P0 변경을 Codex 자기검토만으로 완료 처리하지 않는다.
