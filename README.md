# 앱인토스 소모임 미니앱 — 팀 작업 안내 (START HERE)

> 이 폴더를 처음 받은 팀원용 안내서. 무엇을·어디서·어떻게 작업하는지 한 장으로 정리한다. 자세한 규칙은 `CLAUDE.md`.

## TL;DR (3줄)
1. **시작:** `node tooling/scripts/세션_시작_컨텍스트.mjs` 실행 → `프로젝트_운영/00_팀공유/00_진행사항.md` 읽기.
2. **내 작업:** 역할(A 이상혁 / B 곽소정 / C 김민혁 / D 양록빈)에 맞는 폴더에서(아래 3번 표), 작업지시서 단위로.
3. **막히면:** 30분 이상 막히면 `00_진행사항.md`의 "차단 이슈"에 올린다. 혼자 붙잡지 않기.

## 0. 지금 상태 (2026-06-20, Day 2 / 7일)
- AI 하네스·거버넌스: **구축 완료**(하네스 기본 검증 PASS / 제출용 `--strict`는 제품 산출물 작성 후). 제품 코드: **아직 시작 전**.
- Gate 0(출시 가능성): **Pending** — 앱인토스 MCP 연결·트랙 승인·계정/배포 확인 필요.
- 발표 2026-06-26 14:00 · P0 배포 후보 6/22.

## 1. 여기서 시작하세요 (모든 세션 공통)
1. `node tooling/scripts/세션_시작_컨텍스트.mjs` → 현재 상태·읽을 문서 확인
2. `프로젝트_운영/00_팀공유/00_진행사항.md` — 오늘 현황·차단 이슈
3. `프로젝트_운영/00_팀공유/01_인수인계.md` — 직전 작업 이어받기
4. `프로젝트_운영/00_팀공유/02_팀원_업무배분.md` — 내 역할·오늘 작업
5. 내 작업지시서 (`산출물/09_AI개발파이프라인/작업지시서/TASK-…`)

## 2. 폴더 지도 (무엇이 어디에)
| 폴더 | 용도 | 주로 보는 사람 |
|---|---|---|
| `프로젝트_운영/` | **매일 보는 운영판** — 진행사항·인수인계·업무배분·작업패킷 | 전원 |
| `산출물/` | 제출 산출물(기획·정책·설계·테스트·매뉴얼) | 역할별 |
| `산출물/09_AI개발파이프라인/` | 작업지시서·**독립검토서**·결정요청서(=AI 작업·합의 기록) | 전원·AI |
| `평가증빙/` | AI 활용·사람검수·테스트 증거 | 전원 |
| `발표자료/` | 발표·시연 자료 | A·D |
| `apps/` `packages/` `infra/` | 제품 소스코드 — 예정(apps·packages), 현재 `infra`만 존재 | B·C |
| `.claude/ .agents/ tooling/` | AI 하네스·검증 스크립트 (평소 거의 안 건드림) | D·AI |

## 3. 누가 어디서 (RACI)
| 역할 | 사람 | 주 작업 폴더 |
|---|---|---|
| A 제품·정책·팀장 | 이상혁 | `산출물/01_서비스기획`·`02_정책준수`, `발표자료` |
| B UX·프런트 | 곽소정 | `산출물/03_화면설계`, `apps/miniapp` |
| C 백엔드·데이터 | 김민혁 | `산출물/04_시스템설계`·`05_개발설계`, `apps/api` |
| D QA·릴리즈 | 양록빈 | `산출물/06_테스트`·`07_배포운영`, `tooling` |

## 4. 어떻게 작업하나 (행동 1건 = 1 TASK)
`작업지시서(Ready)` → 구현(Claude 또는 Codex) → **다른 AI 독립검토** → Claude·Codex 토론·합의 → **사람 승인** → 자동 커밋

- **합의가 사는 곳:** `산출물/09_AI개발파이프라인/독립검토서/`(REVIEW-…) + `산출물/00_프로젝트관리/의사결정_기록.md`(DL-…).
- **브랜치:** 작업은 `feature/fix/chore/docs/TASK-…` 브랜치 → `develop` 병합(독립검토 후). `main` 직접 작업 금지(부트스트랩 1회만 예외), `main` 병합은 이상혁 승인+검증+독립검토서. 정본: `.claude/rules/git.md`.
- **커밋:** 자동(conventional 메시지). 헬퍼 `커밋.mjs`는 `--path`로 파일 명시 스테이징(`--all`은 옵트인). **푸시:** 사람 허락, `main` 직접 push 금지(PR).

## 5. 검증 (커밋·제출 전)
```
node tooling/scripts/검증_전체.mjs            # 하네스 기본 검증
node tooling/scripts/검증_전체.mjs --strict   # 제출용(산출물 전수)
```

## 6. 꼭 지킬 것
- 산출물·발표·증빙은 **한글 경로/파일명**. 영문 `docs/` 만들지 않기.
- 비밀값(.env·토큰) 커밋 금지(자동 차단됨).
- 문서의 `사람 확인 전용`(HUMAN_APPROVAL) 구역은 AI가 손대지 않음.
- "완료/통과/배포"는 **실제 실행 증거**가 있을 때만 기록.

## 7. 더 보기
- 최상위 규칙: `CLAUDE.md` · `.claude/rules/`
- 하네스 구조: `산출물/09_AI개발파이프라인/멀티에이전트_하네스_구성도.md`
- 학과 공유용 소개: `산출물/09_AI개발파이프라인/하네스_소개.md` · 라이프사이클: `…/라이프사이클_흐름.md` · 평가: `…/평가_25점_대응표.md`
- 전체 산출물 목록·상태: `산출물/00_프로젝트관리/산출물_목록.md`
