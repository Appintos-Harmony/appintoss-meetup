# 루프 실행 로그 — TASK-20260623-010 (루프 엔지니어링 구축)

- 작업 ID: TASK-20260623-010
- 최종 status: AI Review (사람 머지·푸시 대기)
- 실행: Claude Code (Opus), 게이트는 tooling/scripts가 코드로 강제
- 일시/환경: 2026-06-23 · Windows 11 · Node v24.16.0 · 브랜치 `chore/TASK-20260623-010-loop-engineering`
- 베이스/결과 커밋: `a4be0aa`

## 확인한 입력
- 워크플로 타당성 분석(12에이전트, 판정 Conditional YES)
- CLAUDE.md §7(상태기계·결과계약), `.claude/rules/git.md` DL-012(커밋=자동/푸시=사람), `documents.md`(HUMAN_APPROVAL 보호)

## 변경 파일 (전부 allowed_paths 안, 제품 코드 무변경)
- `tooling/scripts/작업_인덱서.mjs` `루프_가드.mjs` `루프엔진_검증.mjs` · `커밋.mjs`(--guard-task 추가)
- `tooling/scripts/__tests__/루프엔진.test.mjs`
- `.claude/commands/loop-engineer.md`
- `평가증빙/루프실행_로그/README.md`
- `산출물/09_AI개발파이프라인/작업지시서/TASK-20260623-010_…md`, `…/루프엔지니어링_설계.md`, `…/독립검토서/REVIEW-20260623-035_…md`
- `.gitignore`
- (폐기) `코드게이트.mjs` — REVIEW-035 후 중복으로 삭제. 코드 게이트는 `검증_전체 --code` 사용.

## 실행한 명령과 실제 결과
| 명령 | 결과 | 종료코드 |
|---|---|---|
| `node tooling/scripts/루프엔진_검증.mjs` | 단위테스트 16/16 PASS(독립검토 회귀 포함) | 0 |
| `node tooling/scripts/검증_전체.mjs --code` | 문서·링크·비밀값 + tsc + vitest **58/58 PASS** | 0 |
| `node tooling/scripts/작업_인덱서.mjs` | TASK 파싱(누락 0) · Ready 식별 | 0 |
| `커밋.mjs … --path …` | pre-commit 비밀값 스캔 PASS, 1차 커밋 `a4be0aa` | 0 |

## 자동 검증이 잡은 구현 오류 (CLAUDE.md §19.1 사례 5)
구축 중 두 건은 사람 눈이 아니라 **실행 게이트가** 먼저 잡았다. 둘 다 같은 세션에서 수정 후 재검증 통과.
1. **프런트매터 파서**: `작업_인덱서.mjs`가 처음엔 TASK 17건을 0건으로 읽음. 실제 파일이 CRLF + 구형 ```yaml 펜스 포맷이라 선행 `---` 정규식이 빗나감. → LF 정규화 + 양쪽 포맷 지원으로 수정, 17/17 파싱.
2. **가드 자기참조 오탐**: `루프_가드.mjs`가 `HUMAN_APPROVAL` 마커 리터럴을 코드에 담아 **자기 자신**을 보호파일로 STOP 처리. check-diff 실행에서 드러남. → 마커를 동적 생성(`['HUMAN','APPROVAL','START'].join('_')`)해 자기참조 제거. 단위테스트로 회귀 고정.

## 실패·생략한 검증 (정직)
- 무인 야간 cron 실증: **Not Run** (P4, 사람 승인 전 보류).
- Codex 진짜 교차검토: **Not Run** (사람 구동 대기). 본 빌드의 독립검토는 Claude 워크플로(same-family) 초안 — 게이트를 닫지 못함.
- `apps/api`·packages·infra 자동 게이트: 없음 → check-diff의 `isUngatedCode`가 STOP(사람 검증, 설계대로).

## 남은 위험
- 무인 실행 시 `git checkout`이 settings 허용목록에 없어 멈춤 → 사람이 `Bash(git checkout:*)` 추가 필요(AI 자가확대 불가).
- 선택은 lowest-id 단조라 큰 코드 TASK가 먼저 잡힘 → 감독 주간 권장.

## 다음 사람 확인 항목
1. develop 머지·푸시 (현재 푸시 미실행, 사람 게이트).
2. Codex 독립 교차검토 구동.
3. (무인 승급 시) settings.json 권한 1줄.

## 후속 — 독립검토(REVIEW-035) 발견 blocker 조치
1차 커밋(`a4be0aa`) 직후 적대적 독립검토 워크플로(4렌즈)가 **실증된 blocker 11건**을 찾았다. 핵심: ① 코드게이트가 빈 staging에 `--staged`를 불러 tsc/vitest가 매 루프 미실행(no-op PASS), ② `커밋.mjs`가 보호경로 미확인, ③ 게이트 스크립트(`tooling/scripts`) 자체가 보호경로에 없어 루프가 자기 게이트 수정 가능, ④ 대소문자 무시 FS 우회(`.CLAUDE/**`), ⑤ 글롭 경계 누수, ⑥ forbidden_paths 미강제, ⑦ 반복 캡 우회 등. 검토 중 사람이 병렬로 `코드검증.mjs`(항상 실행 코드게이트)를 커밋(`7f3c5fc`)한 것도 드러나 내 `코드게이트.mjs`와 중복임이 확인됐다.

조치(전부 코드+회귀테스트):
- 중복 `코드게이트.mjs` **폐기** → 루프는 `검증_전체 --code`(항상 실행) 사용 [B1·B6·B10].
- 공유 `canonicalize()`(소문자·역슬래시·`..`)로 모든 경로 비교 일원화 [B4·traversal].
- `PROTECTED_PATHS`에 `tooling`·`.claude`·`.git`·`.gitignore` 등 디렉터리 전체 추가 [B3].
- `withinAllowed` 세그먼트 경계, `forbidden_paths` 강제, `touchesHumanApproval`(HEAD+신규+END), 단조 캡 [B5·B7·B8·B11].
- `커밋.mjs --guard-task`로 커밋 경계 재강제(위반 시 자동 unstage) [B2 부분].
재검증: `루프엔진_검증` 16/16, `검증_전체 --code` PASS, 익스플로잇 재현 전부 닫힘. 상세 [REVIEW-035](../../산출물/09_AI개발파이프라인/독립검토서/REVIEW-20260623-035_루프엔지니어링_검토.md). 잔여: B2 완전 백스톱(pre-commit 훅 P2)·사람 구동 Codex 교차검토.

---

## 부록 A — 라이브 엔진 실행 transcript (1차 커밋 a4be0aa 시점 캡처 · 코드게이트는 이후 폐기)

```
### [0] ITERATION — 반복 캡
$ node tooling/scripts/루프_가드.mjs --iteration
# 루프 가드 — iteration
반복 1/8 진행 가능.

### [1] SELECT — 다음 Ready 작업 결정론 선택
$ node tooling/scripts/작업_인덱서.mjs --next
{ "next": { "id": "TASK-20260622-030", "status": "Ready",
            "allowed_paths": ["apps/miniapp/src/**"], ... },
  "safety": { "safe": true, "violations": [] } }            (종료코드 0)

### [2] PREFLIGHT — 안전 점검 (TASK-003)
$ node tooling/scripts/루프_가드.mjs --preflight --task TASK-20260623-003
GO: 브랜치 chore/… · TASK-20260623-003 Ready · 경로안전 통과.   (종료코드 0)

### [5a] 코드게이트 — 문서만 변경 → 자동 생략
$ node tooling/scripts/코드게이트.mjs --changed 산출물/06_테스트/결과.md
대상 변경: 1건 — 코드 경로(apps/**) 없음 → 게이트 생략(PASS).   (종료코드 0)

### [5b] 코드게이트 — apps/api 변경 → HARD STOP
$ node tooling/scripts/코드게이트.mjs --changed apps/api/server.mjs
HARD STOP: 자동 게이트가 없는 코드 경로 변경(apps/api/** 등). 사람 검증 필요.  (종료코드 2)
```

재현: 위 명령을 저장소 루트에서 그대로 실행하면 같은 결과가 나온다(결정론).
