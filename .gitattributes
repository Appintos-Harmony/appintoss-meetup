# Linux에서 실행되는 셸 스크립트·systemd·nginx 설정은 항상 LF.
# (Windows에서 만들어 CRLF가 섞이면 서버 bash/systemd/nginx가 깨짐 — 2026-06-24 배포 사고 재발 방지)
*.sh           text eol=lf
*.service      text eol=lf
*.conf         text eol=lf
deploy/*       text eol=lf
tooling/git-hooks/* text eol=lf
