# 최신 인수인계 (기술용 동기화본)

> 사람용 정본은 [01_인수인계](../00_팀공유/01_인수인계.md). 이 파일은 기술 담당자용 최신 포인터만 유지한다(중복 금지, AI 규칙 §5·§7.5).

| 항목 | 내용 |
|---|---|
| 최신 인수인계 ID | HANDOFF-20260622-fullstudio-sync |
| 작성자 | Codex |
| 대상 | 하모니 풀스튜디오 + AWS API + 운영 문서 정본 동기화 |
| 업무 상태 | 구현 통합 완료, 출시 전 검증·앱 등록 대기 |
| 브랜치·커밋 | `review/fullstudio` @ `6372dc3` |
| 검증 기준 | 단위 58/58 PASS, `build:web` PASS, API smoke PASS, `검증_전체` PASS |

## 다음 사람 핵심 행동 (요약)

1. `node tooling/scripts/세션_시작_컨텍스트.mjs` 실행 후 실제 Git 상태 확인.
2. `apps/miniapp/.env`에 `VITE_API_BASE=https://3.39.167.74.nip.io` 설정 후 빌드.
3. 앱인토스 콘솔에서 하모니 앱 등록 후 `apps/miniapp/granite.config.ts`의 임시 `appName='meetup-lite'`를 실제 등록 ID로 교체.
4. 출시 전 `apps/miniapp/src/lib/identity.ts`를 실제 `getAnonymousKey`로 교체(TASK-20260622-033).
5. 실기기 검수표 RT-01~15를 사람 폰에서 Pass/Fail로 갱신.
